# this directory needs to exist for build artifacts to populate it
# Ignore everything in this directory
*
# Except this file
!.gitignore
# and the empty js folder, required for create_messages.py
!js/
